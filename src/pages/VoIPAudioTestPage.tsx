import React from 'react';
import VoIPAudioTest from '../components/VoIPAudioTest';

const VoIPAudioTestPage: React.FC = () => {
  return (
    <div className="min-h-screen bg-gray-800 flex items-center justify-center p-4">
      <VoIPAudioTest />
    </div>
  );
};

export default VoIPAudioTestPage;